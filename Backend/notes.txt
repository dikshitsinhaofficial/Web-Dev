const { setMaxIdleHTTPParsers } = require("http")


right file Sync find the right file and add the code there

about utf version and encoding in node js
In Node.js, you can specify the encoding when reading or writing files. The most common encoding is UTF-8,
 which is a widely used character encoding that can represent almost all characters in the Unicode standard.

iteraterator

connect the form html to backend json file and add the data to json file

readUser saveUser schema for getting data

status code 200 for success and 400 for bad request and 500 for server error
process.argv
jwt for authentication 
no expiration time for token
ui setMaxIdleHTTPParsers hjk
security breah from ui side
jwt will create new token encrypted
user defined token no0t used in 0real life 
fetch function
